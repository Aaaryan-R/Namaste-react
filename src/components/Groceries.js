const Groceries = () => {
    return (
        <>
            <p>This is the groceries page</p>
        </>
    );
};

export default Groceries; /* Should be a default export for it to work with lazy() and <Suspense></Suspense> */